# The core scorecard uses only the Python standard library.
# Optional packages for notebooks and extended feature workflows:
pandas>=2.2
scikit-learn>=1.5
numpy>=2.0
matplotlib>=3.9
jupyter>=1.0
duckdb>=1.0
pyyaml>=6.0
