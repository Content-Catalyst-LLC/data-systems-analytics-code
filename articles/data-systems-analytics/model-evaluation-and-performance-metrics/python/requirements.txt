# The core scorecard uses only the Python standard library.
# Optional packages for notebooks and extended model evaluation:
pandas>=2.2
scikit-learn>=1.5
matplotlib>=3.9
jupyter>=1.0
duckdb>=1.0
pyyaml>=6.0
